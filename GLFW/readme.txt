This folder should contain build scripts and such for the GLFW platform (Linux/Windows/Mac/...). 
I suggest using foreign calls into C for the MEnv functions, as in the iOS platform. And OpenGL would probably 
be easiest if using OpenGL 3.3+, since that is similar to OpenGL ES 2.0
